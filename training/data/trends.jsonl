{"instruction": "Compare this month to last month", "output": "SELECT (SELECT SUM(amount) FROM transactions WHERE date >= date('now', 'start of month')) as this_month, (SELECT SUM(amount) FROM transactions WHERE date >= date('now', '-1 month', 'start of month') AND date < date('now', 'start of month')) as last_month", "category": "trends"}
{"instruction": "Is subscriptions spending going up?", "output": "SELECT strftime('%Y-%m', date) as month, SUM(amount) as total FROM transactions WHERE category = 'Subscriptions' GROUP BY month ORDER BY month DESC LIMIT 3", "category": "trends"}
{"instruction": "Spending trend", "output": "SELECT strftime('%Y-%m', date) as month, SUM(amount) as total FROM transactions GROUP BY month ORDER BY month", "category": "trends"}
{"instruction": "Is dining spending going up?", "output": "SELECT strftime('%Y-%m', date) as month, SUM(amount) as total FROM transactions WHERE category = 'Dining' GROUP BY month ORDER BY month DESC LIMIT 3", "category": "trends"}
{"instruction": "Monthly spending trend", "output": "SELECT strftime('%Y-%m', date) as month, SUM(amount) as total FROM transactions GROUP BY month ORDER BY month", "category": "trends"}
{"instruction": "Is healthcare spending going up?", "output": "SELECT strftime('%Y-%m', date) as month, SUM(amount) as total FROM transactions WHERE category = 'Healthcare' GROUP BY month ORDER BY month DESC LIMIT 3", "category": "trends"}
{"instruction": "Is entertainment spending going up?", "output": "SELECT strftime('%Y-%m', date) as month, SUM(amount) as total FROM transactions WHERE category = 'Entertainment' GROUP BY month ORDER BY month DESC LIMIT 3", "category": "trends"}
{"instruction": "What's my average monthly spending?", "output": "SELECT AVG(monthly_total) as avg_monthly FROM (SELECT SUM(amount) as monthly_total FROM transactions GROUP BY strftime('%Y-%m', date))", "category": "trends"}
{"instruction": "Dining spending trend", "output": "SELECT strftime('%Y-%m', date) as month, SUM(amount) as total FROM transactions WHERE category = 'Dining' GROUP BY month ORDER BY month DESC LIMIT 6", "category": "trends"}
{"instruction": "Dining trend", "output": "SELECT strftime('%Y-%m', date) as month, SUM(amount) as total FROM transactions WHERE category = 'Dining' GROUP BY month ORDER BY month DESC LIMIT 6", "category": "trends"}
{"instruction": "Is travel spending going up?", "output": "SELECT strftime('%Y-%m', date) as month, SUM(amount) as total FROM transactions WHERE category = 'Travel' GROUP BY month ORDER BY month DESC LIMIT 3", "category": "trends"}
{"instruction": "Is gas spending going up?", "output": "SELECT strftime('%Y-%m', date) as month, SUM(amount) as total FROM transactions WHERE category = 'Gas' GROUP BY month ORDER BY month DESC LIMIT 3", "category": "trends"}
{"instruction": "Utilities spending trend", "output": "SELECT strftime('%Y-%m', date) as month, SUM(amount) as total FROM transactions WHERE category = 'Utilities' GROUP BY month ORDER BY month DESC LIMIT 6", "category": "trends"}
{"instruction": "Shopping spending trend", "output": "SELECT strftime('%Y-%m', date) as month, SUM(amount) as total FROM transactions WHERE category = 'Shopping' GROUP BY month ORDER BY month DESC LIMIT 6", "category": "trends"}
{"instruction": "Month over month spending", "output": "SELECT strftime('%Y-%m', date) as month, SUM(amount) as total FROM transactions GROUP BY month ORDER BY month DESC LIMIT 6", "category": "trends"}
{"instruction": "Is shopping spending going up?", "output": "SELECT strftime('%Y-%m', date) as month, SUM(amount) as total FROM transactions WHERE category = 'Shopping' GROUP BY month ORDER BY month DESC LIMIT 3", "category": "trends"}
{"instruction": "Is my grocery spending going up?", "output": "SELECT strftime('%Y-%m', date) as month, SUM(amount) as total FROM transactions WHERE category = 'Groceries' GROUP BY month ORDER BY month DESC LIMIT 6", "category": "trends"}
{"instruction": "This year vs last year", "output": "SELECT (SELECT SUM(amount) FROM transactions WHERE strftime('%Y', date) = strftime('%Y', 'now')) as this_year, (SELECT SUM(amount) FROM transactions WHERE strftime('%Y', date) = strftime('%Y', date('now', '-1 year'))) as last_year", "category": "trends"}
{"instruction": "Entertainment spending trend", "output": "SELECT strftime('%Y-%m', date) as month, SUM(amount) as total FROM transactions WHERE category = 'Entertainment' GROUP BY month ORDER BY month DESC LIMIT 6", "category": "trends"}
{"instruction": "Travel spending trend", "output": "SELECT strftime('%Y-%m', date) as month, SUM(amount) as total FROM transactions WHERE category = 'Travel' GROUP BY month ORDER BY month DESC LIMIT 6", "category": "trends"}
{"instruction": "Is transport spending going up?", "output": "SELECT strftime('%Y-%m', date) as month, SUM(amount) as total FROM transactions WHERE category = 'Transport' GROUP BY month ORDER BY month DESC LIMIT 3", "category": "trends"}
{"instruction": "Subscriptions spending trend", "output": "SELECT strftime('%Y-%m', date) as month, SUM(amount) as total FROM transactions WHERE category = 'Subscriptions' GROUP BY month ORDER BY month DESC LIMIT 6", "category": "trends"}
{"instruction": "Gas spending trend", "output": "SELECT strftime('%Y-%m', date) as month, SUM(amount) as total FROM transactions WHERE category = 'Gas' GROUP BY month ORDER BY month DESC LIMIT 6", "category": "trends"}
{"instruction": "Am I spending more than last month?", "output": "SELECT (SELECT SUM(amount) FROM transactions WHERE date >= date('now', 'start of month')) as this_month, (SELECT SUM(amount) FROM transactions WHERE date >= date('now', '-1 month', 'start of month') AND date < date('now', 'start of month')) as last_month", "category": "trends"}
{"instruction": "Is groceries spending going up?", "output": "SELECT strftime('%Y-%m', date) as month, SUM(amount) as total FROM transactions WHERE category = 'Groceries' GROUP BY month ORDER BY month DESC LIMIT 3", "category": "trends"}
{"instruction": "Average weekly spending?", "output": "SELECT AVG(weekly_total) as avg_weekly FROM (SELECT SUM(amount) as weekly_total FROM transactions GROUP BY strftime('%Y-%W', date))", "category": "trends"}
{"instruction": "Average daily spending?", "output": "SELECT AVG(daily_total) as avg_daily FROM (SELECT SUM(amount) as daily_total FROM transactions GROUP BY date)", "category": "trends"}
{"instruction": "Is utilities spending going up?", "output": "SELECT strftime('%Y-%m', date) as month, SUM(amount) as total FROM transactions WHERE category = 'Utilities' GROUP BY month ORDER BY month DESC LIMIT 3", "category": "trends"}
{"instruction": "Groceries spending trend", "output": "SELECT strftime('%Y-%m', date) as month, SUM(amount) as total FROM transactions WHERE category = 'Groceries' GROUP BY month ORDER BY month DESC LIMIT 6", "category": "trends"}
{"instruction": "How does this month compare to average?", "output": "SELECT (SELECT SUM(amount) FROM transactions WHERE date >= date('now', 'start of month')) as this_month, (SELECT AVG(monthly_total) FROM (SELECT SUM(amount) as monthly_total FROM transactions GROUP BY strftime('%Y-%m', date))) as average", "category": "trends"}
{"instruction": "Transport spending trend", "output": "SELECT strftime('%Y-%m', date) as month, SUM(amount) as total FROM transactions WHERE category = 'Transport' GROUP BY month ORDER BY month DESC LIMIT 6", "category": "trends"}
{"instruction": "Healthcare spending trend", "output": "SELECT strftime('%Y-%m', date) as month, SUM(amount) as total FROM transactions WHERE category = 'Healthcare' GROUP BY month ORDER BY month DESC LIMIT 6", "category": "trends"}
{"instruction": "Year over year comparison", "output": "SELECT strftime('%Y', date) as year, SUM(amount) as total FROM transactions GROUP BY year", "category": "trends"}
