{"instruction": "Impact of canceling Netflix", "output": "SELECT SUM(amount) * 12 as yearly_savings FROM transactions WHERE description LIKE '%NETFLIX%' AND date >= date('now', '-1 month')", "category": "complex"}
{"instruction": "What if I cut dining by 50%?", "output": "SELECT SUM(amount) * 0.5 as potential_savings FROM transactions WHERE category = 'Dining' AND date >= date('now', '-3 months') / 3", "category": "complex"}
{"instruction": "Can I afford a $500 purchase?", "output": "SELECT CASE WHEN (SELECT SUM(balance) FROM v_current_balances WHERE type = 'checking') > 500 THEN 'Yes' ELSE 'No' END as can_afford, (SELECT SUM(balance) FROM v_current_balances WHERE type = 'checking') as available", "category": "complex"}
{"instruction": "Days until I'm broke at this rate", "output": "SELECT ROUND((SELECT SUM(balance) FROM v_current_balances WHERE type = 'checking') / (SELECT AVG(daily_total) FROM (SELECT SUM(amount) as daily_total FROM transactions WHERE date >= date('now', '-30 days') GROUP BY date)), 0) as days_remaining", "category": "complex"}
{"instruction": "Spending velocity this month", "output": "SELECT date, SUM(amount) OVER (ORDER BY date) as cumulative_spending FROM transactions WHERE date >= date('now', 'start of month')", "category": "complex"}
{"instruction": "Discretionary vs essential spending", "output": "SELECT CASE WHEN bucket = 'needs' THEN 'Essential' ELSE 'Discretionary' END as type, SUM(t.amount) as total FROM transactions t JOIN budget_categories bc ON t.category = bc.category GROUP BY type", "category": "complex"}
{"instruction": "Projected month-end spending", "output": "SELECT ROUND((SELECT SUM(amount) FROM transactions WHERE date >= date('now', 'start of month')) * 30.0 / CAST(strftime('%d', 'now') as REAL), 2) as projected_total", "category": "complex"}
{"instruction": "3-month average spending", "output": "SELECT AVG(monthly_total) as avg_3mo FROM (SELECT SUM(amount) as monthly_total FROM transactions WHERE date >= date('now', '-3 months') GROUP BY strftime('%Y-%m', date))", "category": "complex"}
{"instruction": "What's my savings rate?", "output": "SELECT ROUND((SELECT SUM(amount) FROM transactions WHERE category IN ('Savings', 'Investments', '401k')) * 100.0 / (SELECT SUM(amount) FROM transactions WHERE amount > 0), 1) as savings_rate", "category": "complex"}
{"instruction": "6-month average by category", "output": "SELECT category, AVG(monthly_total) as avg_6mo FROM (SELECT category, strftime('%Y-%m', date) as month, SUM(amount) as monthly_total FROM transactions WHERE date >= date('now', '-6 months') GROUP BY category, month) GROUP BY category ORDER BY avg_6mo DESC", "category": "complex"}
